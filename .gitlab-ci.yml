stages:
  - pages

pages:
  stage: pages
  image: python:3.11-slim
  script:
    - python -m pip install -r requirements.txt
    - python generate.py
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
